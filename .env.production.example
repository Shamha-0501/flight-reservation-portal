# Production Docker env for the frontend container.
# Replace YOUR_SERVER_IP with the public IP of the server running the backend container.

NEXT_PUBLIC_ROOT_DOMAIN=
NEXT_PUBLIC_APP_PORT=3000
NEXT_PUBLIC_API_ORIGIN=http://20.2.138.95:8000
NEXT_PUBLIC_RESERVED_SUBDOMAINS=www,api

HOSTNAME=0.0.0.0
PORT=3000
NODE_ENV=production
PLATFORM_ROOT_DOMAIN=
