import { RouteLoadingScreen } from "@/src/shared/components/common/RouteLoadingScreen";

export default function Loading() {
  return <RouteLoadingScreen title="Loading admin workspace" description="Preparing tenant and platform management screens." variant="dashboard" />;
}
