import { RouteLoadingScreen } from "@/src/shared/components/common/RouteLoadingScreen";

export default function Loading() {
  return <RouteLoadingScreen title="Searching flights" description="Preparing flight results and booking options." variant="dashboard" />;
}
